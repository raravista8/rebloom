# VISUAL_COVERAGE — live tracker (canon-import vs hand-rolled)

> Per-screen / per-component status across viewports. Update on every canon vendoring (`CANON_PACKAGE_TZ.md §9`) and every UI task (CLAUDE.md UI DoD).
> Сейчас: canon `0.9.2` вендорен и рендерится в `web/` (Next.js 15 + Playwright-харнесс поднят, smoke зелёный). Экраны постепенно подключаются к API; per-screen pixel-baselines добавляются по мере готовности (T2.2). 0.6.2 — лендинг-only: **синхронизирован герой** (бровь «Люди передаривают свои букеты», H1 «…напрямую от людей…», статичное фото `hero-lacybird.png`, ценник «17 200 ₽ → от 4 500 ₽ / −74%», «128 букетов рядом») — раньше прод рендерил старый герой, т.к. `LandingHome.tsx` строился по устаревшей мета-таблице §8.3; **десктопный поповер выбора города** (`NavCity` — port canon-референса, Esc/клик-вне, → /[slug]). CSS доезжает пакетом; JSX-правки портированы вручную в `LandingHome.tsx` (web хэндроллит лендинг, не импортит canon `PdLanding`). 0.6.3 — фикс спейсинга заголовков (двухклассовые `.pdl-sechead .pdl-h2/.pdl-sub` перебивают `.pd-root` ресет) + полировка секций (шаги/эскроу/возражения/финал/фильтры-мобайл) — всё через CSS; портированы JSX: текст шага 1, блок доверия `.pdl-esafe-h` (+ убрана stale-escrow-копия «деньги возвращаются»), пол в отзывах (Артём/Юлия). 0.7.0 — **мобильное бургер-меню** (`components/marketing/MobileMenu.tsx` — порт canon `PdMobileMenu`/`.pdl-drawer`, гость, реальные роуты; **портал в `<body>` + `position:fixed`**, т.к. `.pdl` с `container-type` иначе якорит drawer к полному документу, а не к вьюпорту); фикс отзывов Тимур/Марина (убрана escrow-копия). Бургер на canon-SEO-страницах (`.pds`/`.pdc`, импортятся целиком) скрыт CSS — у canon `PdMobileMenu` зашиты `.html`-ссылки без route-props; ценник «комиссия 5%» из canon НЕ портирован (конфликт с ADR-0013, площадка денег не касается). 0.8.0 — **экспорт пришёл docs-only** (без src/dist), поэтому правки применены web-side: фото на экране логина (`.pad-photo` → `public/hero-lacybird.png` в `LoginFlow`), снятие завершающих точек в заголовках/подзаголовках (6 строк: auth ×3, лендинг ×3 — остальные web-экраны уже чистые), и 3 canon-CSS фикса (A6 спейсинг согласия, A7 выравнивание чекбокса, C `PdNotice text-wrap:pretty`) в `globals.css` как consumer-override (до полного re-export). OTP-клавиатура у web уже системная (`autocomplete=one-time-code` + `inputmode=numeric`, без кастомного нумпада). **Заблокировано**: точки на SEO-страницах (`PdGeoPage`/`PdSafeDeal` импортятся целиком — нужен полный canon src) и OAuth-марки провайдеров (web-auth беднее canon-прототипа). 0.8.1 — **полный пакет** (не docs-only): все правки 0.8.0 теперь в `src/`+`dist/canon.css`, поэтому web-override сняты (блок A6/A7/C из `globals.css`, rewrite фото в `next.config.mjs`). Чузер логина обновлён через canon-импорт: официальная **T‑ID-марка** (`img/oauth/tid.svg`), разделитель «или», провайдеры без пред-выделения, фото `img/hero-lacybird.png` (оба ассета → `web/public/img/`). **Снят прежний блок**: точки на SEO-подзаголовках (`pds-h2-sub` ×5) теперь правятся в источнике `seo.jsx`; точки в settings (×5) и admin-mobile (×3) тоже в источнике; `AuthOtpFill` экспортирован. canon 0.8.1 сделал `menuLinks` **обязательным** пропом SEO-шеллов → прокинуты реальные guest-роуты (общий `GUEST_MENU_LINKS` из `MobileMenu.tsx`); сам бургер на `.pds`/`.pdc` пока скрыт CSS (нужна проверка containment абсолютного drawer на не-`.pdl` корне + per-page baseline). Пере-генерён pixel-baseline `login chooser` (mobile-375). 0.8.2 — **SafeDeal no-escrow фикс (закрыт долг SEO-аудита 2026-06)**: `src/marketing/seo.jsx` `PdSafeDeal` «Почему так безопаснее» — два эскроу-H3 («Когда деньги уходят/возвращаются продавцу», «срок ожидания», «спор с фото», авто-релиз) заменены на «Несколько простых правил» + «Если что-то не так» (не пришёл/не тот → просто не платите, возвращать нечего; жалоба → предупреждение/ограничение/блок + снятие объявления; репутация берёт следующих). Урок-грабли: **prototype↔canon drift** — прототип починили ещё в 0.8.1, но canon-`src` (что реально вендорит web) — нет, и прод тянул старый текст; web импортит `PdSafeDeal` из **`dist/marketing.mjs` → пересборка dist обязательна** (старый текст сидел в dist `\uXXXX`-эскейпами, grep по кириллице его не видел). Только текст в той же раскладке → визуал ≤2% (safe-deal покрыт лишь геометрия-гардом `desktop.spec.ts`, не скриншотом). ТЗ выполнено: `docs/handoff/canon-tasks/safedeal-noescrow-copy.md`. **0.9.0 — design-pass (метро / фильтры / карточки / листинг / форма / единая шапка).** Бэкенд уже отдаёт `metro:{id,name,lines[{name,color}]}|null` + `flower_types[]` в `listing_card`/`listing`, `/api/search` принимает повторяемые `metro`/`flower` и отдаёт `total`. Порт (всё руками, web хэндроллит эти экраны): **типы** `metro`/`flower_types`/`total`/`SearchParams` + `lib/feed.ts` (повторяемые `metro=`/`flower=`); **`BouquetCard`** — «Размер L» тихий, **метро вместо города** (`MetroLabel` — реюз `.pd-metro`/`.pd-mdots` с живыми цветами линий, т.к. canon-`PdMetroLabel` ищет цвет по line-id, а бэк отдаёт hex; fallback на город при `metro:null`), рейтинг вплотную к имени; **`ListingDetail`** — «Букет M», город+метро («Самовывоз у м. …»), блок «Продавец» (`.pd-sellercard`: «Продаёт …», «рейтинг продавца», сделки отдельной статой), состав текстом через «·», тихая `.pd-sharebtn`, **цена убрана с CTA**; **`SellForm`** — `MetroPicker` вместо «Района» + опц. `FlowerPicker`, «Когда букет подарили», убран хинт «700–1 300 ₽», POST шлёт `metro_station_id`+`flower_types`; **`SearchScreen`**+**`/catalog`** — общий слой фильтров (`components/catalog/`: метро-мультивыбор + тип-цветов + размер-с-подписями + дропдаун «Сортировка ▾»/сегмент, моб. панель «Все фильтры» с «Показать N» по `total`); **`/catalog`** — реальный browse-first экран (`/api/feed` база + `/api/search` при фильтрах, cursor load-more, все состояния, PUBLIC). **Единая шапка `PdWebNav`** в `WebChrome` (auth-aware через `useMe()` сохранён: гость → «Войти», без избранного/аватара, `null`→guest без флэша); десктоп-корни несут `.pdl` (нужен для container-query показа десктоп-контролов + containment драйвера), каталог/поиск-десктоп вкладывают `.pdc` в `.pdl`. **Ключевые грабли/находки:** (1) canon `PdMetroPicker`/`PdFlowerPicker` жёстко завязаны на свои RU-name/label-списки и **отдают имена/лейблы**, а бэк валидирует/фильтрует по стабильным **id** → построены web-пикеры (`components/forms/`) поверх тех же `.pd-mpick*`/`.pd-flowerpick` классов, отдающие id (катало­ги `lib/metro.ts`+`lib/flowers.ts` зеркалят бэкенд). (2) canon `PdCatalog` — **демо без data-пропсов** (свои данные, нет item/handler) → не годится для live, реюз разметки `.pdc-*`. (3) `PdWebNav` принимает только `{active,authed,city,user,links,onPublish}`; бренд/«Войти»/город/поиск/звонок/аватар без хендлеров → click-capture-адаптер в `WebChrome` маршрутизирует canon-статичные `.html`/`#login` и кнопки в реальные Next-роуты. **«Избранное»-сердце** в шапке у canon — авторизованная аффорданс; гостевой лендинг её не несёт (matched canon guest-nav). **Открытые хвосты (бэкенд, вне скоупа порта):** нет API-эндпоинта каталога метро (зеркалим `metro.py` статикой — при дрейфе обновить); `PublicUser` не отдаёт `created_at` → «N месяцев на площадке» рендерится только когда поле появится. Pixel-baseline'ы под Linux-регенерацию: **home-landing**, **listing-detail** (реально изменились). `login-chooser` — только macOS-шумовой дифф (≈3%), не трогал. **0.9.1 — фиксы по TZ:** `PdWebNav` `cityLoc` → «в Москве» (грамматика, меняет desktop-1280 baseline home-landing/listing-detail); `PaymentFailed` ре-экспорт убран **в источнике** (`entries/deal.jsx`) — canon-сборка чистая без consumer-fix; 8 состояний коллекции в CSS. **0.9.0-хвосты ЗАКРЫТЫ (бэкенд):** `GET /api/geo/metro` отдаёт каталог станций → web `lib/metro.ts` теперь fetch+per-city-кэш (зеркало удалено); `created_at` в `PublicUser` → «N месяцев на площадке» оживает; демо-букеты забэкфилены метро+цветами. **`/catalog` остаётся ХЭНДРОЛЛОМ:** canon 0.9.1 `PdCatalog` стал презентационным (пропсы), НО его `PdCard` рендерит `src={img/{id}.jpg}` (`feed.jsx:183`) + демо-лайк без `onLike`/render-prop → импорт = битые фото листингов + мёртвые лайки (хуже хэндролла со своим `BouquetCard`). Ждём data-driven `PdCard` (флаг `canon-tasks/canon-0.9.2-pdcard.md`); до тех пор `CatalogScreen.tsx` хэндроллит `.pdc-*` с настоящими фото+лайками. **0.9.2 — хэндролл `/catalog` СНЯТ.** `PdCard` стал data-driven (фото из полного URL, лайк наружу), а `PdCatalog` получил **`renderCard`-slot** + клиентскую сортировку — поэтому `CatalogScreen.tsx` теперь **импортит `PdCatalog`** и кормит его live-данными, а карточку отдаёт через **`renderCard={(item)=><BouquetCard variant="grid"/>}`** (web сохраняет свою карточку: настоящее `photo_thumb_url` + рабочий лайк `LikeButton`→POST /like, гость→/login + метро-лейбл). Canon-`PdCard`/`onLike` не используются (renderCard short-circuit'ит до них). **Маппинг:** фильтры держатся в форме `PdCatalogFilters` (PdCatalog владеет UI фильтров) → `/api/search` (рубли→копейки для цены; metro/flower/size/freshness — passthrough). `rating`-фильтр **inert** (бэкенд не фильтрует по рейтингу продавца — не шлём, не переключает feed→search); `cheap`/`exp`/`rating`-сортировки — **клиентские** в `PdCatalog` (по `item.price` рубли + `item.seller.r`), поэтому live-item адаптируется: полный `ListingCard` + `price=price_kopecks/100` + `seller.r=seller_rating`. **Грабли:** PdCatalog гейтит «Показать ещё» на `items.length < total`, не на курсоре → для browse-ленты (нет общегородского `total`) шлём `total = max(real_total, items.length + (cursor?1:0))`, чтобы load-more оставался виден; при исчерпании курсора `total==items.length` и `state='end'`. **`header`-slot** = `<WebChrome cityId/>` (auth-aware `PdWebNav` через `useMe()` сохранён); canon-CSS уже знает композицию `PdWebNav внутри .pdc--desk` (`.pdc--desk .pdl-nav-mid/-fav/-cta{display:flex}`, бургер скрыт) + `@container pdc (min-width:900px)` — десктоп-шапка/сайдбар раскрываются. **`CatalogFilters.tsx`/`filters.ts` ОСТАВЛЕНЫ** — их импортит `/search` (`SearchScreen`); удалять нельзя. Спека: `tests/visual/catalog.spec.ts` обновлён под DOM `PdCatalog` (`.pdc .pdc-grid .pd-card`, empty «Пока нет букетов», end «Показали все букеты», public). `package-lock.json` бампнут 0.9.1→0.9.2 (install). Visual ≤2% — функционал-спеки зелёные; pixel-baseline `/catalog` под Linux ещё нет (только desktop-геометрия-гард в `desktop.spec.ts`).
> Legend: 🔵 spec only · 🟢 canon-import + visual ≤2% · 🟡 hand-rolled (justify) · 🔴 missing/broken

## Screens
| Screen | Mobile (375) | Mobile (414) | Desktop | Visual baseline | Status |
|---|---|---|---|---|---|
| Главная / витрина | 🔵 | 🔵 | 🔵 | — | spec |
| Карточка букета | 🔵 | 🔵 | 🔵 | — | spec |
| Продать | 🔵 | 🔵 | 🔵 | — | spec |
| Вход (OTP) | 🔵 | 🔵 | 🔵 | — | spec |
| Сделка + чат | 🔵 | 🔵 | 🔵 | — | spec |
| Отзыв | 🔵 | 🔵 | 🔵 | — | spec |
| Профиль | 🔵 | 🔵 | 🔵 | — | spec |
| Админ/модерация | n/a | n/a | 🔵 | — | spec |

## Components (canon)
| Component | Status | Notes |
|---|---|---|
| Button / IconButton | 🔵 | all states required |
| Input / Textarea / Select / OtpInput | 🔵 | |
| BouquetCard (+FreshnessBadge,+LikeButton) | 🔵 | hero component |
| FreshnessIndicator | 🔵 | colour + icon (a11y) |
| PhotoUploader | 🔵 | 1–5, progress |
| DealStatusStepper | 🔵 | escrow states |
| ChatBubble / ChatInput | 🔵 | |
| RatingStars / ReviewItem / SellerBadge | 🔵 | |
| BottomNav / TopAppBar / FeedSectionHeader | 🔵 | |
| Money / Badge / Avatar / Skeleton / Toast / Modal / BottomSheet | 🔵 | |
