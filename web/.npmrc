# Materialize the vendored canon (file:../packages/canon) as a real package copy,
# not a symlink — bundlers (Next/tsup) don't traverse file: symlinks, and CI's
# `npm ci` must reproduce the same tree. (CANON_PACKAGE_TZ.md §9 step 5.)
install-links=true
